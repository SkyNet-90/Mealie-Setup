############################################################
#  Mealie Stack — Environment Variables
# ─────────────────────────────────────────────────────────
#  1. Copy this file  →  .env
#  2. Fill in every value marked REQUIRED
#  3. In Portainer, paste these into the "Environment
#     variables" section (or upload .env via "Load variables
#     from .env file").
#
#  ⚠️  NEVER commit a filled-in .env to Git.
#      Only this .env.example template is committed.
############################################################

# ── Mealie image tag ────────────────────────────────────
# Pin to a specific version for stability, or use "latest"
MEALIE_IMAGE_TAG=v3.10.2

# ── General ─────────────────────────────────────────────
BASE_URL=https://recipes.jo-jo.net
TZ=America/New_York
TOKEN_TIME=48
PUID=1000
PGID=1000

# ── Default admin (change password on first login!) ─────
DEFAULT_EMAIL=changeme@example.com
DEFAULT_GROUP=Home
DEFAULT_HOUSEHOLD=Family

# ── Security ────────────────────────────────────────────
SECURITY_MAX_LOGIN_ATTEMPTS=5
SECURITY_USER_LOCKOUT_TIME=24

# ── Cloudflare Tunnel  ──────────────────────────────────
# REQUIRED — grab from Cloudflare Zero Trust → Tunnels
CLOUDFLARE_TUNNEL_TOKEN=CHANGEME

# ── Watchtower ──────────────────────────────────────────
# How often (seconds) to check for image updates (86400 = 24h)
WATCHTOWER_POLL_INTERVAL=86400

# ── SMTP / Email (optional) ────────────────────────────
# Uncomment the matching lines in docker-compose.yml too
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_AUTH_STRATEGY=TLS
# SMTP_FROM_NAME=Mealie
# SMTP_FROM_EMAIL=you@gmail.com
# SMTP_USER=you@gmail.com
# SMTP_PASSWORD=your-app-password
